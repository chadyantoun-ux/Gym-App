# Backlog

Owned by `project-manager`. Seeded from the first code review of `index.html` (2026-09-09).
Severity: **P0** = loses or corrupts data · **P1** = gives wrong advice or blocks real use ·
**P2** = real but survivable · **P3** = architecture debt.

Status: `open` · `specified` · `in-progress` · `qa` · `done` · `wontfix`
`specified` = a work order exists with acceptance criteria; no code written yet.

Work orders live in `docs/work-orders/`.

| WO | Covers | Status |
|---|---|---|
| [WO-001](work-orders/WO-001-p0-data-loss.md) | B-01, B-02, B-03, B-21 | merged and deployed |
| WO-002 (planned) | B-04 + B-16 — the backup round trip | not written |
| [WO-003](work-orders/WO-003-advice.md) | B-30, B-17(sort), B-24, B-08, B-25, B-06, B-07, B-22, B-12, B-23, B-26, B-09, B-18 + B-32, B-20, B-33, B-38, B-27, B-28, B-41, B-42 | **engine half built and signed off** (W1/W2/W5/W6/W7/W9/W12/W14/W19/W21). Frontend half — W3/W4 specs written; W8/W10/W11/W13/W15/W17/W18/W20 **superseded by WO-004**. W16 (the S1 notice engine) was never built and is re-homed to WO-004 W4. |
| [WO-005](work-orders/WO-005-overnight.md) | The overnight run of 2026-09-11 — an **execution plan**, not new scope. Sequences the unbuilt WO-004 items into a serial `index.html` chain plus parallel lanes, files B-65…B-68, rules the five items Chady cannot answer while asleep, and defines the "operational" checklist and the stop conditions | executing |
| [WO-004](work-orders/WO-004-redesign.md) | The redesign. B-46 … B-61, plus B-13, B-14, B-15, B-23, B-26, B-27, B-28, B-29, B-31, B-33, B-34, B-35, B-36, B-37, B-43, B-44 and every held WO-003 frontend item | **M0–M2 merged to `main` (5f4e21e).** M3 (W14–W15) was cut by WO-005 against Chady's explicit "build it now" decision (B-69), rebuilt under WO-006 and **merged to `main` @ `bad454b`**. Rows still reading `specified — WO-004 W#` are unreconciled statuses, not unbuilt work — PM to reconcile in the next pass |
| [WO-006](work-orders/WO-006-plan-editor.md) | **W14 Plans list + W15 Plan Editor**, as they ship now, plus the `hydrateDraft()` fix (B-70), the restore-side plan guard (B-71) and the E-3 criteria WO-004 predates (plans back up through `save()`, restore brings them back). Owns the record of the two process errors (B-69) and the rule that closes them | **closed 2026-09-12** — merged to `main` @ `bad454b`, deployed to production (eleven files byte-verified against the merge commit, live `sw.js` `v4`, offline cold load green), suite **634 / 634 / 0**. W1 `91a597c` → W2 `8cba070` ∥ W3 (coach, K2 applied `9e0834a`) → W4 `95829dd` (**not a pass**: B-73 P1 + two P2s) → fixes `3bd35ac` → W4 second pass `44e8f91` (**pass**, found B-74 P1) → fix `7c8daf5` → W5 `5e2f45d`. Closed B-70, B-71, B-73, B-74; filed B-75–B-82. B-69 is *recorded and ruled*, not resolved by this close; B-72 stays open |
| [WO-007](work-orders/WO-007-resplit.md) | **Re-split: move an exercise between days keeping its id**, delete an empty day, demote `Build from empty` below `Duplicate PHAT and rearrange`. Chady's ask of 2026-09-12: compose push/pull/legs out of PHAT's slots without building from empty. Files B-83–B-87; nine coaching questions to `strength-coach` first (mixed-role days, speed work beside its source lift, `phatProvenance` blind to day membership, the per-day reintro counter). | **specified 2026-09-12** — no code. W1 coach ∥ W2 ux ∥ W3 backend (coach-independent half) → W3 rest → W4 frontend → W5 QA → W6 release. PM recommendation on the record: ship **after** the first logged session, under PHAT as-is, because ids are preserved and nothing logged now is lost by the re-split later. His call; four questions in §7 |
| [WO-008](work-orders/WO-008-two-users.md) | **Two users: a login and a log each for Chady and Diana.** Chady's ask of 2026-09-12. Flags the conflict with `CLAUDE.md:3` ("One user") and §7 (the diet numbers are his); the forcing question is **same phone or two phones**, default two. Files B-88 (cross-account push — live today), B-89 (identity on screen + first-run sign-in), B-90 (advice to a person the app has no numbers for), B-91 (same-phone profiles, conditional), B-92 (two sign-ups then the lock), B-93 (per-user diet targets, filed not built). Six coaching questions on what in the advice layer belongs to a person. | **specified 2026-09-12** — no code. W1 coach ∥ W2 ux ∥ W3 release run **now**; W4 backend waits for WO-007 W4 (one writer on `index.html`) → W5 frontend → W7 QA → W8 release. W6 (same phone) only on Chady's answer. PM position: the sixth tool for zero sessions; log one first, nothing here loses it |

**Suite tripwire.** `tests.html` is **`634 / 634 / 0`** on `main @ bad454b` (WO-006; 605 after W1, 625 after W2, 631/630/1 at QA's first pass with B-73 red on purpose). Earlier: **`465 / 465 / 0`** on `wo-004-redesign` (was 456/456/0 before coach §9.12/§9.13, 347 on `wo-003-advice`, 148/147/1 on `main`); 49 of the new ones cover WO-004 W2. **The one red is deliberate and is a work item, not a carried-forward failure:** coach §9.13.3 (2026-09-10) struck `A training week is 3, so week 1 starts when you get there.` in favour of `3 sessions logged. Week 1 starts when three land in one week.`, and `logic.js` has not followed yet. The regression test was written before the fix so the fix is provable (same shape as B-38). Owner **backend**, `logic.js` → `tierLines()`; QA verified by simulation that changing that one string plus `TW0LINE()` in `tests.html` returns the page to **464 / 464 / 0**. Ten mutants injected against the §9.12 role-gate and day-one work, ten killed. Was **`347 / 347 / 0`** on `wo-003-advice` (was 148/147/1 on `main`). 134 of those cover the nine rule engines; 12 of 13 injected mutants were killed and the survivor was proved an equivalent mutant, not a coverage hole.
**Zero expected failures is enforced by the suite itself** — two meta-tests fail on any `known bad` /
`xfail` test name, or on any skip without a stated reason. The earlier `N ≥ 236` target is superseded: it
assumed a frontend that is now deliberately on hold.

**Signatures in WO-003 are stale; the code is right.** `volumeTier` and `deloadCheck` take a context
object (the positional lists could not satisfy their own criteria) and `stallReport` takes a fourth
`state` argument that Rule E2 requires and no document specifies. PM to restate — do not "fix" the code
back to the work order.

---

## P0 — data integrity

| ID | Issue | Owner | Status |
|---|---|---|---|
| B-01 | **An in-progress session exists only in memory.** `S.draft` is never persisted. Reload, phone lock-and-kill, or an accidental back gesture loses the whole workout. This is the single worst bug in the app. Draft must autosave on every keystroke under its own key and be offered back on boot. | backend + frontend | **done — WO-001, merged and deployed** |
| B-02 | **Malformed numbers silently delete a set.** `syncSet` strips to `[\d.]` but allows `"7.5.0"`; `+"7.5.0"` is `NaN`, so `done()` filters the set out and `finish()` saves the session *without it*. No warning. The user sees their set vanish after saving. | backend + frontend | **done — WO-001, merged and deployed** |
| B-03 | **`today()` uses UTC.** `new Date().toISOString().slice(0,10)` returns the UTC date. Logging late at night or early morning writes the wrong calendar day, breaks "Done today", and can create two bodyweight rows for one day. Must use local date parts. | backend | **done — WO-001, merged and deployed** |
| B-04 | **Export with no import.** The UI says "Export once a month so nothing costs you the history" — but there is no way to load a backup back in. The backup is decorative. Add import with validation and a merge/replace choice. | backend + ux | open — **WO-002**, with B-16. **New constraint from WO-003:** the importer must accept `schemaVersion` **2 and 3**. **WO-004 adds a third:** schema **4**, the plan document plus `planId` on sessions. The importer must accept **2, 3 and 4**. |
| B-46 | **The redesign keys exercises by NAME.** `docs/design/PHAT App.dc.html:451` keys them `slug(name)+":"+kind`, so with the plan editor's rename a typo orphans an exercise's whole history — `lastFor`, `stallReport`, `speedLoad`, `e1rmByDate`, `liftDays`, `painWindow` and `volumeTier` all read history by id. The design's own note ("Trend keeps comparing whatever lift names you keep") acknowledges the trade instead of solving it. It also collapses the two "Skull crusher" slots (d1h 3×6–10 power, d5i 3×12–15 hyp) into one history. **Ruled in WO-004 C-6: identity is a stable opaque id, never name-derived, never rewritten by a rename.** The design's trend requirement is kept by a separate `lift` grouping field. | backend | **built — WO-004 W2 (`wo-004-redesign`), awaiting QA.** Opaque ids, the `lift` grouping field, schema 4 and the plan document are in `logic.js`. The log is empty today, which is the only reason this was cheap |
| B-47 | **The redesign renumbers day ids.** `d1, d2, d4, d5, d6` — "Back & shoulders" moves d3→d4, "Lower hypertrophy" d4→d5. Stored sessions carry `dayId`, and worse, `logic.js` has these ids compiled in: `REINTRO_ORDER` (line 2088) keyed `d1..d5`, `SPEED_SRC` (line 1898) `{d3a:"d1a", d4a:"d2a", d5a:"d1d"}`, `KEY_LIFTS` `d1a/d1d/d2a/d2d`, and the stored `reintro` counter keyed by dayId. Renumbering silently re-points the speed-work source lift and the accessory ramp onto the wrong days. **Ruled in WO-004 C-5: storage ids do not move.** The design's numbering is a display artefact of showing the rest day; the correct expression is B-31's weekday labels | backend | **built — WO-004 W2 (`wo-004-redesign`), awaiting QA.** Storage ids do not move: the shipped plan document carries `d1..d5` / `d1a..d5j` unchanged, plus a `wd` weekday label per day, which is B-31's home |
| B-48 | **`− REMOVE` destroys a typed set with no confirmation.** The prototype's `removeSet` pops the last row unconditionally, including one holding a completed `120 × 5`, with no confirm and no undo. Same failure class as B-02 and B-21. **Ruled in WO-004 C-12:** removing a set with any typed value needs an explicit confirmation naming what is deleted, and is undoable; a blank set goes silently; the button prefers the last **blank** set, not the last row | frontend + ux | **specified — WO-004 W7** |
| B-49 | **The sample-data loader can destroy the real log.** `loadDemo` overwrites sessions and bodyweight with 30 generated sessions; `startFresh` empties both; `toggleDemo` sits in Settings as `SWITCH TO AN EMPTY LOG` — one tap, no confirmation. And demo sessions are byte-identical to real ones on disk, so a fabricated session can drive a verdict, a stall report and a deload trigger. **Ruled in WO-004 C-14:** separate store key, `demo:true` on every sample session, the real log never written while demo mode is on, and no control anywhere empties the real log without a typed confirmation | backend + frontend + release | **specified — WO-004 W13** |
| B-21 | **A 0 kg set is silently deleted.** `done()` requires `+s.w>0`, so a rack chin or unweighted dip logged as `0 × 10` disappears on save. Same failure mode as B-02, different trigger. | backend | **done — WO-001, merged and deployed.** Advice-layer sibling B-32 is open. |

## P1 — wrong advice or blocked use

| ID | Issue | Owner | Status |
|---|---|---|---|
| B-05 | **No edit or delete of a saved session.** One fat-fingered `140` instead of `100` permanently distorts the trend chart, the stall detector and every future verdict, with no way to correct it. | frontend + backend | open — **severity rises with WO-003**: nine rules now read that history and act on it confidently. First in the queue behind WO-002. |
| B-06 | **Bodyweight rate uses entry count, not dates — and the calorie bands contradict the brief.** `b.slice(-7)` takes the last 7 *entries*, which can span a month. Separately and independently, the bands are wrong: the app cuts 200 kcal above +0.35 kg/wk where the brief says do nothing below +0.5, triggers "add" below +0.2 where the brief says *flat*, and prints "Gaining too slowly" when you are actually **losing**. The panel also prints "Target: 0.2–0.3" under logic using 0.35. Spec: **Rule W1**. Acceptance: 8 entries over 24 days shows **no** rate and **no** advice. | backend + strength-coach | **specified — WO-003 W7 (engine) + W8 (Weight tab)** |
| B-07 | **Stall detector measures weight only, and never re-baselines.** ~~Compares against the all-time first session~~ — struck: that *is* the brief's week-6 protocol, deliberately. The real defects: it reads `topSet`, so week 1 `100×3/3/3` → week 6 `100×5/5/5` reports no progress and blames intensity or diet, punishing the user for obeying the app; and the baseline never rolls forward, so by week 30 it can never fire. Fix: estimated 1RM over two rolling 21-day blocks. Spec: **Rule ST1**. Acceptance: that 3/3/3 → 5/5/5 case produces **no** warning. | strength-coach + backend | **specified — WO-003 W9 (ST1) + W10 (Trend tab)** |
| B-08 | **Progression recommends off the top set.** `verdictFor` power branch uses `topSet(sets)` as the current load. Do 100/100/95 and it recommends 102.5 kg — based on a weight you did not hit on every set. Working load is the `min` of the first `ex.s` completed sets. Spec: **Rule P1**. Acceptance: `100/100/95` at 5/5/5 never recommends 102.5 kg. | strength-coach + backend | **code on `wo-003-advice` — WO-003 W6.** `PHAT.verdict` uses `min` of the first `ex.s` completed sets; `100/100/95` renders the not-matched copy. Awaiting QA (W21) and coach sign-off on the seven calls in `decisions.md`. |
| B-22 | **Dumbbell load unit is undefined.** The app never states whether a DB weight is per dumbbell or per pair. Makes the "Bench" trend line uninterpretable and blocks B-10 and B-12. Ruled: **per dumbbell**, stated in the target line. No migration needed — no data logged yet. | strength-coach ✅ · frontend | **specified — WO-003 W11.** Implement tags ruled in W2. **Blocks W12/SP1** (WO-003 §H6). |
| B-23 | **"Full volume" checkbox misrepresents the programme.** An all-or-nothing toggle cannot express the brief's ramp: drop ✂ for weeks 1–4, then reintroduce **one accessory per session from week 5**, only if recovery holds. Replace with a per-day reintroduction counter and a weekly gated offer, rolled back automatically when the stall detector fires. Spec: **Rule V1**. | frontend + backend | **WO-003 W14 (engine) is built. The UI moves to WO-004 W13, where the checkbox is DELETED and not replaced.** The redesign ships the checkbox again — with helper text that describes the correct per-session ramp. The design contradicts itself; the ruling is the tiebreak (WO-004 C-1). |
| B-24 | **Verdicts fire mid-exercise.** `paintVerdict` runs on every keystroke, so one completed set of three prints `Volume down 66%. Add a rep or 2.5 kg next time.` while the user is still working. Gate every verdict on `completed sets >= ex.s`. Cheapest correctness win in the app. | frontend + backend | **code on `wo-003-advice` — WO-003 W5.** `PHAT.verdict` returns `null` below `ex.s` completed sets, and `paintVerdict` now runs on commit only (stepper tap, focusout, clear, first paint), never on `input`. Awaiting QA. |
| B-25 | **Hypertrophy verdict ignores the rep range.** Compares tonnage only, so 3×20 on an 8–12 exercise scores as progress. Spec: **Rule H1**. | strength-coach ✅ · backend | **code on `wo-003-advice` — WO-003 W6.** Range compliance first (H1 cases 1-2), then Z3's basis test, then tonnage. Awaiting QA. |
| B-32 | **A saved 0 kg set reads back as "Stay at 0 kg".** B-21 is genuinely fixed in the data layer — a rack chin stores as `w:0` — but `topSet()` still filters `+s.w>0`, so the verdict says `Stay at 0 kg until all 3 sets reach 10 reps`. Wrong advice on correct data. `tests.html` carries this as a KNOWN-BAD test that documents the wrong output and asserts only that storage stays right. **Do not close it by dropping the set again.** | strength-coach → backend | **code on `wo-003-advice`: Z1/Z2 implemented, `0×10, 0×9` on d1c now renders `Stay at bodyweight until all 2 sets reach 10 reps.` and no rule can produce the string `0 kg`. The 42 implement tags (Rule I1) shipped with it, data only. Was: WO-003 W2 (ruling) → W6 (code) → W21 (the KNOWN-BAD test is rewritten, not deleted).** The audit's literal `completed = w > 0` would reintroduce B-21 one layer up — see WO-003 §H3. `lastFor`'s `+x.w>0` is the same bug and is fixed in W1. |
| B-26 | **Pain is collected and ignored.** The note placeholder invites "RIR, form, pain, anything" and the app then recommends +2.5 kg regardless. Suppress load increases and show a fixed referral line. Not the app's call to assess. Spec: **Rule S1**. | ux + backend | **specified — WO-003 W16 + W17.** The suppression *mechanism* moves earlier, into W5/W6 — see §H4. |
| B-50 | **The redesign's verdict gate is `min(ex.s, sets.length)`.** Remove two sets from a 3-set exercise and the verdict fires on one completed set — **B-24 reopened through a new door**, and B-24 was the cheapest correctness win in the app. **Ruled in WO-004 C-11:** the gate is `completedSets >= ex.s` against the **prescription**, always. Removing sets does not buy a verdict | frontend | **specified — WO-004 W7** |
| B-51 | **The redesign's calorie decision is B-06 verbatim.** `bwAvgN(7)` takes the last 7 *entries*, not the last 7 dates, and the bands are `>=0.2 && <=0.35 correct · >0.5 cut · <0.15 add` — the exact bands coach-audit §2 struck down. It also prints a kg/week number from two entries (violating `wo-003-train-weight.md` §0.1 #6) and labels the average with no day count (#7). **Ruled in WO-004 C-9:** `PHAT.calorieAdvice` and `PHAT.bwWindows` ship verbatim with the insufficient-data state and the `7-day average (6 of 7 days)` label | frontend | **specified — WO-004 W11** |
| B-52 | **The redesign shows the wrong week, twice.** `weekNo()` is calendar weeks from the first session; every gated rule uses `PHAT.trainingWeeks` (weeks containing ≥ 3 training days). The Monday-start ruling requires both numbers on screen when they differ. And the design puts a second programme-state line (`testCountdown`) beside the first, which `wo-003-train-weight.md` §0.1 #4 forbids. **Ruled in WO-004 C-10:** `PHAT.cycleLine` owns the single line | frontend | **specified — WO-004 W9** |
| B-53 | **The redesign has no note field on any screen.** It is Rule S1's only input (`PHAT.painFlag` reads the note), it is what B-26 and the unbuilt WO-003 W16 hang off, `validateDraft` deliberately keeps a notes-only entry because a typed note is data, and `wo-003-session-screen.md` §0.4 makes it the **last input on the card** — the whole bottom-stack safety rule is defined relative to it. **Ruled in WO-004 C-15: the note field returns** | ux + frontend | **specified — WO-004 W5 + W7** |
| B-54 | **The redesign has no refusal state at all.** No blocked-save, no malformed-set error, no storage-failure notice. WO-001's B-02 mechanism — the thing that stops a set vanishing — has nowhere to render, and `wo-003-session-screen.md` §0.1 #6 requires advice and refusal to be visually distinguishable | ux + frontend | **specified — WO-004 W5 + W8** |
| B-55 | **The redesign reclassifies 8 exercises from `power` to `hyp`** — d1c, d1f, d1g, d1h, d2c, d2e, d2f, d2g. **CORRECTED: `d2b` hack squat was never reclassified** — `PHAT App.dc.html:471` has it as `power`; this work order conflated it with `d4b`, the Lower-hypertrophy hack squat, which is `hyp` in both files. An engineer transcribing the work order rather than the source would have flipped a slot, producing a programme change that originated in a bug report. — and drops two alternates ("Glute-ham raise or lying leg curl" → "Lying leg curl", "DB row or shrug" → "DB row"), while reopening B-28. `k` selects P1 vs H1 **and** R1's rest row, so this is a programme change wearing a mock-up's clothes. `strength-coach` verified all 42 slots against the handoff brief; that verification does not survive this | strength-coach → Chady | **RULED — rejected.** Chady: the brief is the programme. Coach §8a: **the day heading governs** — `k` is a progression-protocol routing tag, not a claim about adaptation, and the brief assigns the protocol per day. Flipping would drop power-day rest to 90 s ready / 120 s cap, below the 2–3 min minimum the brief itself states. The design's own split is not internally consistent either (it keeps weighted pull-up, weighted dip and hack squat as `power` at the same 6–10 reps), so there was no coaching position to rebut. |
| B-56 | **Rest is per-day in the design, per-role in the ruling — and R1 has no engine.** The prototype sets `rest: 150/180/90` on the day; Rule R1 is per exercise (`power hi<=8` 150/180 · `power hi>8` 120/180 · `hyp hi<=12` 90/120 · `hyp hi>12` 60/120 · `speed` 60, hard cap 90). The day number is wrong for a cambered bar curl and a lateral raise, and would give speed work 90 s of *ready* where 90 s is the hard cap. R1 was WO-003 W18, a frontend item, and was held — so no engine was ever built | backend + strength-coach | **engine built — WO-004 W4.** `PHAT.restTarget(ex)` → `{ready,cap,hard}` and `PHAT.restText(ex, elapsedSeconds)` → `{text,state,stopped,…}`, both pure, both reading `ex.k`/`ex.hi` and nothing else. `rest` is deliberately **not** a field on a day. Frontend seam (W7) outstanding |
| B-57 | **The plan editor produces exercises the rules cannot read.** A user-created exercise has no `k` (which rule fires, which rest applies), no `implement` (Z2's load word, I2's increment line) and no `cut` (V1). The prototype's `addEx` hard-codes `("New exercise", 3, 8, 12, "hyp")` — a silent guess that decides what the app tells him to lift. And SP1, V1, ST1 and D1 are keyed to PHAT-specific ids a user plan does not have. **Ruled in WO-004 C-7:** `k` and `implement` required at creation; the four plan-specific tables move into the plan document; on a plan that does not declare them the feature goes silent and says so | backend + strength-coach | **done — WO-006, on `main` @ `bad454b`.** Data half: the four tables are fields on the plan document and each of SP1/V1/ST1/D1/`cycleLine` has a named ABSENT state (coach's literals, `8cba070`). UI half: the add-exercise form refuses `ADD` naming the empty field until `k` and `implement` are answered (B2), the Type line states the consequence of the option chosen and warns once on a mismatched rep range (coach Rule K2, `9e0834a`), and the ABSENT lines render once, on the Plans screen only (B4, W3 confirmed) |

| B-65 | **`d3d` still carries a forbidden slash-name.** `PHAT_PLAN` slot `d3d` reads `n: "DB row or shrug"`. Rule A1 (addendum §8.2) already classed two exercises in one slot as two histories and resolved `d2e` on exactly that ground; `d3d` was left for Chady and never answered. One history cannot describe both. **Defaulted in WO-005 §4.3 to `DB row`** — the design's choice, the cue already written for it, and the only slot where nothing else moves (`implement:"db"`, `lift:"l_dbrow"`, `k:"hyp"`, `cut:1` are correct for either). **Reversible at a cost of two strings:** `n` → `Shrug` and the cue → `Keep the arms straight, no rolling the shoulders.` Zero data impact while the log is empty | backend (default) → Chady (confirm) | **defaulted — WO-005 Wave 0a.** Awaiting Chady's confirmation; the revert is a lookup, not a judgement |
| B-66 | **`index.html` declares its own `PROGRAM` and it contradicts the plan document.** `index.html:508` is a full 42-slot copy; `logic.js` has `PHAT.PHAT_PLAN`. The screens read the copy, the engines are handed the document, and they have diverged: `d2e` is `Glute-ham raise or lying leg curl` / `implement:"bodyweight"` on screen versus `Lying leg curl` / `implement:"machine"` in the plan — so **Rule Z2 prints the wrong load word and Rule I2's increment line is switched off on a machine exercise, today**. The copy also has **no `cue` field on any of 42 slots**, so all 43 signed-off cues are unreachable (commit `ca5e5ae` says as much in its own message), no `lift` (blocks W10's grouping) and no `wd` (blocks B-31). `KEY_LIFTS` still reads `Bench` / `Deadlift` (B-27). Directly contradicts the 2026-09-10 ruling "No slot id survives in code outside the plan document" | frontend | **specified — WO-005 Wave 1a.** Highest-value item of the overnight run and small: delete both arrays, read `PHAT.PHAT_PLAN`, or keep `PROGRAM` only as a commented derived read |
| B-67 | **Archivo is inlined but never linked.** `assets/archivo-inline.css` is in the repo and precached by `sw.js`, but `index.html` has no `<link>` and no `@import` — B-63 shipped the asset and not the typeface. The consequence beyond the look: **every 44 px and 200 %-zoom measurement in W6 and W7 was taken on the fallback stack**, and Archivo has different metrics, so the accessibility floor is unverified against the face that will ship | frontend | **specified — WO-005 Wave 1a.** Lands early and deliberately, so the night measures once against the real face rather than twice |
| B-73 | **A plan saved after a corrupt-store boot got no `recover:plans` copy on restore.** `restoreSteps` keyed the plans keep on the store's BOOT status and `S.stores` was never updated by a later `save()`; a plan built after an unparseable `phat:v1:plans` boot was replaced by a restore with no copy on any key. Found by QA (WO-006 W4 first pass `95829dd`, written red as the fix's proof). Fixed on both halves in `3bd35ac`: `logic.js` keys the keep on CONTENT (`planStoreHolds`: a stored plan or a non-PHAT active id), `index.html` `save()` marks the store `ok` after a successful write. Read off a paused call frame in DevTools, "Already proven" item 30; pinned S33 | backend | **done — WO-006, on `main` @ `bad454b`** |
| B-74 | **Restore keeps, the REPLACE sheet's count and Export read MEMORY, not disk.** `restoreApply` handed `restoreSteps` `logPayload()/bwPayload()/plansPayload()` from `S`, `restoreStart` counted `S.sessions`, and `exportAll()` exported `S`. In one document memory equals disk; across two (the installed app plus a browser tab) tab B's Restore said "replaces 5 sessions", kept 5, wrote no `recover:plans`, and tab A's later session and plan were afterwards on **no key on disk**. He typed REPLACE on a false number. **Pre-existing since E-3** — the log keep was always memory. Found by QA W4 second pass `44e8f91`; fixed in `7c8daf5`: `storesOnDisk()` does one `readRaw` per store before the sheet and again before the keeps; the count, the keeps, the empty-or-typed decision and the pre-restore export all come from those bytes; a store whose read fails refuses the restore by name with nothing written; Export reads disk too and names any store it had to take from memory. Two-tab Playwright: sheet says 6, `recover:log` holds 6, `recover:plans` holds A's plan, every keep before the first write | backend | **done — closed before the WO-006 release, on `main` @ `bad454b`.** Sibling hazard on the write side is B-76 |

## P2 — real but survivable

| ID | Issue | Owner | Status |
|---|---|---|---|
| B-09 | **No rest timer.** Per the brief: **power 2–3 min · hypertrophy 1–2 min · speed work 60–90 s.** The most-used feature of every gym app and it's absent. Must store an absolute timestamp so it survives backgrounding, and must never block or delay a set save. Spec: **Rule R1**. Needs B-19 first. | frontend + ux | **specified — WO-003 W18.** Gets a **narrow** render-free `textContent` seam, not a B-19 fix. If that seam cannot be built cleanly, R1 is the first item cut from the batch (§H5). |
| B-10 | **No plate math.** "Go 102.5 kg" without telling you what to load per side. Blocked by B-22 (dumbbell unit); scope to barbell exercises. | frontend + strength-coach | open — **unblocked by WO-003 W11**, but not built there. Needs its own work order. |
| B-11 | **Chart spaces points by index, not by date.** A two-week layoff renders identically to back-to-back sessions, which makes the trend line misleading — the one thing a trend line must not be. | frontend | open |
| B-12 | **Speed-day percentage is static text.** "65–70% of your power-day top set" — and "top set" is not a 3–5RM, so the current copy is wrong as well as inert. Compute the kg from the heaviest set at 3–5 reps on the mapped power lift within 28 days, widening to 56, else stay silent. Spec: **Rule SP1**. Blocked by B-22. | frontend + backend | **specified — WO-003 W12 (engine) + W13 (card).** Cannot ship before B-22/W11 (§H6). |
| B-13 | **Accessibility.** `maximum-scale=1` blocks pinch-zoom (never do this); inputs have no labels; `−`/`+` buttons are unlabelled to a screen reader; up/down verdicts are conveyed by colour alone; toast and verdict have no `aria-live`. | ux + frontend | **built — WO-004 W6 (`wo-004-redesign`), awaiting QA.** `maximum-scale=1` removed; every input labelled; every glyph button named (`Weight up 2.5, Bent-over row set 1`); one live region, `#bs-live`, outside `#view` — the toast lost its own `aria-live` and routes through it, and the verdict now announces there on commit. Measured at 400 px: 0 controls under 44 x 44 on all six routable screens, 0 unnamed |
| B-14 | **`fmt()` drops the year.** Ambiguous the moment history passes 12 months. | frontend | **built — WO-004 W6.** `fmt()` appends the year when the date is not in the current calendar year: `4 Sep` / `11 Dec 2025` |
| B-15 | **Duplicate SVG `marker` ids.** `ah_${pattern}` repeats when two diagrams of the same pattern are open — invalid DOM, tolerated today, will bite. | frontend | **built — WO-004 W6.** `ah_${pat}_${++figSeq}` — unique per rendered instance |
| B-16 | **Blob download has no fallback.** `exportAll` relies on a synthetic `a.click()` that silently does nothing in some WebViews — including the native wrapper the `window.storage` bridge implies. Needs a clipboard/share fallback and a visible success state. | frontend + release | open — **pulled into WO-002 with B-04**: an export that can't download and can't re-import is one broken round trip |
| B-17 | **Can only log today, and sessions are never sorted.** No date picker, and `S.sessions` relies on append order, so `lastFor()`'s scan-backwards assumption breaks on any import or backdated entry. **Raised toward P1:** `weeksIn()` reads `S.sessions[0].date` on that unsorted array, and the volume tier, stall test and deload trigger now all gate on a week number. | backend + frontend | **split.** Sorting half: **specified — WO-003 W1**. Date-picker / backdating half: **still open**, unscheduled. |
| B-18 | **No cycle awareness; deload is autoregulated, not scheduled.** Ruled: this programme should **not** get a calendar deload — the ✂ block already is the ramp, and a scheduled deload would put a third of the first 12 weeks at reduced stimulus. Deliver the cycle display first (`Week 7 · full volume phase · 4 of 9 accessories back`), autoregulated triggers second. Spec: **Rule D1**. | strength-coach + backend | **specified — WO-003 W19 (engine) + W20 (banner + cycle line).** Last in the batch; reads B-07 and B-23 output. Second item cut if the batch has to shrink. |
| B-27 | **`KEY_LIFTS` labels are wrong.** "Bench" is a flat DB press; "Deadlift" is a stiff-leg deadlift. The mapping is right, the labels lie. Rename to `DB press` and `SLDL`. | frontend | **specified — WO-003 W10.** Rides along because ST1's new copy names these lifts. |
| B-28 | **`d1a` is named "Bent-over row"**; the brief says "Bent-over or Pendlay row". | frontend | **specified — WO-003 W10.** Rides along because SP1's fallback copy interpolates this name. |
| B-29 | **Diet targets are never displayed.** The app says "add 200 kcal to your training days" without ever showing what the training-day target is. Static panel on the Weight tab: 3,200/170/300/145 training, 2,500/175/60/175 rest, creatine 5 g. | ux + frontend | open |
| B-30 | **`trainingWeeks` does not exist.** `weeksIn()` counts calendar weeks from session one. Rules V1, ST1 and D1 all need "weeks containing ≥ 3 logged sessions". Pure function, testable. | backend | **specified — WO-003 W1.** First item in the batch: every week-gated rule inherits its answer (§H1). |
| B-58 | **A 5 kg increment option in Settings.** `PHAT.round2p5`, Rule P1's step and Rule G1's cap all assume the 2.5 kg grid and CLAUDE.md §3.5 fixes it — but the deeper point is that **the increment is the rule's, not a setting's**: P1 case 3's 2.5 %-per-rep constant and G1's derived step are coaching decisions signed off in `decisions.md`. A Settings control that overrides them lets a UI preference re-prescribe a programme. **Ruled in WO-004 C-3: the section is deleted.** If he wants 5 kg jumps on the big lifts that is a coach-reviewed rule change in its own work order | frontend | **ruled — WO-004 W13** |
| B-59 | **Two contrast failures in the new palette, measured.** Inactive tab labels at `rgba(240,238,234,.42)` on `#1c1b1a` = **3.7 : 1**, below the 4.5 : 1 text floor. The session progress pips at `rgba(240,238,234,.18)` = **1.7 : 1**, below the 3 : 1 meaningful-non-text floor. Everything else in 2a passes and passes well — `.note` at `.55` is 5.4 : 1, `.lbl` at `.5` is 4.7 : 1, amber on graphite is 9.2 : 1. Both failures are one alpha step from fixed | frontend + ux | **built — WO-004 W6, both fixed and a third found.** Inactive tabs `.55` at 11 px (5.3:1, measured). The pips are resolved by FORM, not alpha: the strip is `aria-hidden`, `pipStrip()` carries `Exercise {i} of {n}` as `.sr-only` text, and done/current/upcoming are filled / amber double-height / outline-only, all >= 3:1. **Third failure:** `.lbl` at `.50` is 4.3:1 on `--surface` — the earlier audit measured it on `--bg`. All alphas are now named tokens and text may not go below `.55` |
| B-60 | **The movement illustrations drop to labelled placeholders.** The design ships a 64 px box reading `FIG 3` and the note "supply the movement illustrations and they drop in here". The current app has 15 hand-built SVG patterns (`PAT`/`POSES`, `index.html:374-442`) mapped across all 42 slots. The design does upgrade the cues: 42 per-exercise against today's 15 per-pattern. Losing working artwork for a placeholder is a regression unless deliberate | Chady | **REBUILT — awaiting QA.** The 42 cues landed earlier; the artwork is now re-authored from scratch against §11.5. **The structural defect was the frame, not the drawing:** one 200×150 landscape viewBox for an upright subject, rendered `width:100%` as a banner, so the average figure used 26% of the width and 63% of the height and `legcurl` used 23%. Every pattern now carries its **own** viewBox fitted to its own content and renders compact beside the cue — measured 49–90% of width and 84–89% of height, 4–8 strokes in the solid pose against §11.4's 12. Poses re-authored so the movement is visible: hip travel 17–42% of figure height where the pattern moves the hip, working-end travel 20–81% everywhere. Two authoring rules came out of rendering a contact sheet and looking at it: a segment identical in both poses is drawn **once**, and where the whole body travels the ghost is reduced to the parts that carry the start. `fly` and `legpress` were drawn, rejected and shipped as no figure |
| B-61 | **The redesign's rest timer ticks by full re-render and counts an interval.** The prototype's `setInterval` calls `setState` every second; in this app that is `innerHTML` on the whole view once a second while a thumb is in a weight field — the caret dies every second (B-19). And it decrements a counter rather than storing an absolute start timestamp, so backgrounding the phone loses the count. Both were already ruled in WO-003 W18 and neither survives into the design | frontend | **specified — WO-004 W7.** If the render-free `textContent` seam is not clean, the timer is cut |

| B-69 | **Two process errors on 2026-09-11, filed so they are not lost.** (1) WO-004 asked Chady "build the Plan Editor now, or defer?"; he answered **build it now** (decisions.md 2026-09-10, "Chady's four calls", item 3). WO-005 §5 then cut M3 "restating WO-004's ruling" — it restated the PM's *recommendation*, not his *answer*, and re-litigated a closed decision while he slept. The PM's plan, the PM's error. (2) When he caught it, the main session dispatched `frontend-engineer` on W14+W15 directly, bypassing the PM — a build started with no binding criteria. **Rule, now in decisions.md:** an answered "Needs from Chady" question is closed; a later plan may not reopen it without quoting the answer and stating a new fact. Grep `decisions.md` before writing a cut line. Owned in WO-006 §0 | project-manager | **recorded and ruled — NOT resolved by WO-006's close (2026-09-12).** The rule is in `decisions.md` and WO-006 §0 and stands. **Open ask to Chady:** add the answered-question rule to `.claude/agents/project-manager.md`. He has not said yes; until he does this is an ask on this row, not an edit — a subagent does not amend its own definition. Closes only when he makes (or authorises) that edit |
| B-70 | **`hydrateDraft()` drops draft entries whose exercise is not in the day, and the draft records no `planId`.** `index.html:4037` rebuilds a resumed draft against `PROGRAM` and silently discards any entry whose id the day no longer holds; `saveDraft` (`:3819`) stores no `planId`, so it cannot even hydrate against the plan the session was started under. Unreachable while there was one immutable plan; **a typed-set loss path the moment the editor or a plan switch exists.** Fix: the draft records `planId` (absent = PHAT), hydrates against that plan, and keeps every stored entry — an unknown one renders under its resolved name flagged `Not in the plan any more` and saves with the session | frontend | **done — WO-006 W1 `91a597c`, on `main` @ `bad454b`.** The draft records `planId`, hydrates against that plan (a pre-WO-006 draft with none hydrates against PHAT), and keeps every stored entry: an orphaned set renders last, flagged `Not in the plan any more`, and saves with the session. D8/D9 passed at QA `44e8f91` — the orphan was produced from a second document, since D10 makes the editor unable to produce one (correct, and recorded) |
| B-71 | **Restore keeps no recover copy of the plan store and does not refuse while a plan edit is unsaved.** `restoreApply` (`index.html:4600`) writes `recover:log` and `recover:bw` before replacing, then writes `phat:v1:plans` with **no** `recover:plans`. Harmless while the only plan is code; once a user plan exists a restore silently replaces it with the server's. Same class as the log rule E-3 already enforces, not applied to the third store | backend | **done — WO-006 W2 `8cba070`, on `main` @ `bad454b`.** `PHAT.restoreSteps` returns keeps and writes as two ordered lists; `recover:plans:<ts>` lands through `save()` before `phat:v1:plans` or nothing is written; restore refused under a plan working copy (`Save or discard your changes to <plan> first.`); restored and pushed plan stores are validated and refused-and-named whole. R1–R5 pinned in S32; seam order observed in the browser (item 30). B-73 and B-74 were the two holes QA found in the same seam; both closed before release |
| B-72 | **`PHAT.exName` falls back to the raw id, and no name is recorded on an entry at save time.** `logic.js:5014` returns the id string when no plan holds it — exactly what WO-004 W14 #2 forbids on screen. UX §9.1 assumes "the name recorded at save time", but `buildSession` stamps `rx` (s/lo/hi/k) and no `n`. WO-006 meets the criterion by resolving across every stored plan and rendering `Exercise no longer in any plan` otherwise. Recording `n` per entry is the durable answer; it changes `buildSession`'s key set, which E-3's byte-identical round trip and `orderKeys` assert on, so it is its own small item | backend | open — WO-006 shipped; unblocked. Decide alongside B-75 (both are about what a session records of the plan it was logged under) |
| B-75 | **A draft whose `planId` names a plan the store no longer holds is saved under the ACTIVE plan's id and `rx`.** `finish()` passes `sp().planId` and `sp()`, so a `p_gone` draft is stamped with the active plan's identity and prescription — provenance rewritten, **no set lost**. The pure half (`planIdOf`, `buildSession`) is honest about it and pinned in S33. Reachable only when the STORED plan vanishes between session start and finish (a restore, a hand edit, or a second document); D10 refuses every in-app route. **Blocked on a coaching ruling, not code:** does a session from an unknown epoch carry an `rx` at all, or the vanished `planId` and no `rx` (so `prescriptionEpoch` can never claim it matched)? PM recommendation: the vanished id and no `rx` — the app should not assert a prescription it did not read. Then backend, one call site | strength-coach → backend | open — QA W4 finding, filed at WO-006 close |
| B-76 | **A document's stale memory overwrites a restored log on its next save.** Named by backend while closing B-74. Two documents open: A restores (disk now holds the server's log); B, booted earlier, still holds its boot-time `S.sessions`, and its next `finish()` writes that array plus one session over the restored log. B-74 makes the restore's keep read disk, so `recover:log:<ts>` exists either way and nothing is unrecoverable — but the restore is silently reverted and he sees it only if he looks. Same class as D10 and B-74 (memory guarding a disk fact), on the WRITE side. Fix shape: every store write reads the key first and refuses (naming the reload) when the bytes differ from what this document last wrote or loaded; the decision pure, pinned in `tests.html`. P2 not P1 because a keep exists; do it before any more multi-document surface (B-05 editing history is one) | backend | open — filed at WO-006 close; first backend item on the store layer |
| B-68 | **`manifest.webmanifest` names icons that do not exist.** `assets/README-icons.md` records that Chady is drawing them. An install prompt with missing icons is a degraded install, and — worse — a 404 in the service worker's precache list is a **failed** worker install, because `sw.js` correctly refuses to cache a non-200. The manifest, the precache list and the files on disk must agree | release | **specified — WO-005 §4.6.** Default: generate a 192 px and a 512 px PNG from the app's own tokens (flat `#1c1b1a`, amber `#f5b32b` bar) with Node's built-in `zlib` and no dependency, or drop the icon entries entirely. His artwork later replaces two files and nothing else |

## P3 — architecture debt

| ID | Issue | Owner | Status |
|---|---|---|---|
| B-19 | Full `innerHTML` re-render per view. Fine today because render never fires mid-edit — but focus and caret position will be lost the moment a timer, live sync, or autosave indicator needs to re-render during input. Move to targeted updates before adding any of those. | frontend | open — **constrains WO-001 W5**: autosave must never call `render()`, and no autosave indicator until this is fixed. **WO-003 W18 does NOT fix it** — the rest timer gets one narrow `textContent` channel that never calls `render()`, and B-19 stays open at full scope. |
| B-39 | **A full disk stops the draft saving and says nothing.** Fill storage, keep typing: the draft on disk stays frozen and nothing on screen changes. `saveDraft()` does set `S.err`, but the autosave path is forbidden from calling `render()` (B-19) and nothing else paints it — so he only finds out when he taps Save. Everything typed after the disk filled is lost if the phone dies first. Not a regression (there was no draft at all before WO-001), but it is the one remaining hole in "an in-progress session must survive a phone lock". **Fix: `announce()` already writes to `#bs-live`, which lives outside `#view` — a render-free channel that violates neither B-19 nor the no-autosave-indicator rule.** | frontend | open |
| B-40 | **The draft notice overwrites the more serious store notice.** With site data blocked, boot sets the log-read notice then unconditionally reassigns it for the draft, so the only message shown is "Could not read your saved session. Close the app and reopen it." Meanwhile `blockWrites[LOG]` is silently true, and the advice cannot work when storage is blocked at the browser level. The worst fact should win. | frontend | open |
| B-41 | **W5 criterion 3 overstates the guarantee.** It says at most one in-flight keystroke is at risk; observed, it is the whole field — every keystroke resets the 400 ms debounce, so a field that has never been blurred or stepped is absent from disk until 400 ms after the *last* keystroke. Bounded and small (the field under his thumb; moving on blurs and flushes), but the criterion is what the next change gets measured against. Either restate it or also flush on the first keystroke into an empty field. | project-manager or frontend | **done — restated in WO-003 §Decision 8**, and WO-003 W18 additionally flushes on the first keystroke into an empty field. |
| B-42 | **"`750` unreachable by any route" is false for deletion.** Paste `7.5.0`, backspace twice → `750` on screen. Deletions pass through verbatim by design, and correctly so — stripping them would trap him in a value he cannot edit out of. It can never be *saved* (`750 > 500`). Near-miss worth noting: one backspace gives `75.0`, which saves as `75` and IS in range — visible on screen first, so an editing outcome, not a silent rewrite. **Restate the claim; do not change the code.** | none | **done — restated in WO-003 §Decision 8.** No code change. |
| B-38 | **`buildSession` coerces a non-string `dayId`** instead of returning null as its own doc comment promises: `0` becomes `"0"`, `{}` becomes `"[object Object]"`. Unreachable from the UI today, but it is a defence-in-depth hole in the one function whose job is to refuse rather than guess — a session under an unmatched `dayId` renders as "Session" with its entries unreachable. `tests.html` carries this as a deliberately FAILING test. | backend | **specified — WO-003 W1, rides along.** Rationale (§Decision 1): "expect exactly one failure" is a rule applied from memory, and it is how a second, real failure gets waved through. After the batch the tripwire is `0`. |
| B-45 | **`fail → deload → fail` has no sentence.** It is a distinct state — *the deload did not take* — and the honest response is neither `take a deload week` (T1 correctly resets, per §7.12) nor silence. `strength-coach` named this gap and **deliberately declined to fill it**: it is a new user-facing claim, it sits at the edge of what the app may say, and repeated unexplained strength loss is not something this app assesses. Any copy must point at a person rather than guess (audit §10). What is missing is acknowledgement, not instruction — the card already says `Drop to 115 kg`. **Revisit only once there is real logged history.** | strength-coach | open — deliberately deferred |
| B-64 | **Three exercises are mapped to the wrong movement figure**, and `wo-004-screens.md` §11.5 asserts "the map is correct and is kept". `d3c` seated cable row → `hpull` draws a hinged torso holding a barbell. `d4c` leg press → `squat` draws a standing figure with a bar on its back — a different **ground reference**, which §11.2(1) makes the single thing that renders a pattern legible. `d3d` → `hpull` is wrong too if it becomes a shrug. Found by `strength-coach` while writing the cues, because a wrong figure changes what the cue must carry. §11.8's own rule decides it: **a missing figure is a smaller loss than a wrong one.** Either remap, add patterns, or render nothing for those slots. | frontend + strength-coach | **built with B-60 — awaiting QA.** Ruled: a slot keeps its pattern only while the **ground reference** matches; where only the apparatus changes it keeps it. `d3c` → new `srow` pattern (seat, foot plate, upright torso). `d4c` → **no figure**: a leg press has no fixed ground reference to draw, since the platform is the thing that moves — it was drawn and rejected on the contact sheet. `d3d` → **no figure**: two patterns in one slot until W1 rules the alternate. **Two more found in the same class and closed the same way:** `d5d` incline cable fly → no figure (drawn plan-view, read as a lateral raise — the confusion §11.8(5) forbids); `d5j` rope pressdown → no figure, because `tri` is drawn as a bench skull crusher for d1h/d5i. 38 of 42 slots keep a figure; all four without one keep their cue and render it alone, no box |
| B-62 | **The app has no desktop treatment, and the dock escapes the content column.** `#dock` is `position:fixed; left:0; right:0` while `#view` is capped and centred, so at 1900 px the RESUME button renders ~1870 px wide, the five tabs string across the viewport with ~350 px gaps, and the restore sheet covers the day list. **Shipped behaviour, not an artifact quirk** — any wide screen looks like this. Root cause is honest: every measurement pass ran at 400 px and 200 % text because those are the cases the spec names; **desktop was never specified, so it was never checked.** Ruled: the app renders as a centred column at its design width and does not stretch; the scrim still covers the viewport, because a modal that dims part of the screen is worse than one that dims all of it. | frontend | **in progress** |
| B-63 | **Archivo is named but never fetched, so the design's typography does not ship.** No CDN, offline-first — both correct constraints — but the consequence is that the app falls back to the platform sans on the one device it is for, and the artifact only looks right because it fetches from Google Fonts, which the app cannot do. Fixable within every constraint: inline the face as a base64 woff2 (~30–50 KB, no build step, works from `file://`). | frontend + ux | open — needs Chady |
| B-44 | **`Intl` renders "Sept", the signed-off copy says "Sep" — two date formats in one app, today.** On current ICU (Node 24, recent Chrome) `en-GB` short month is **`Sept`**, so `index.html`'s `fmt()`/`dstr()` already print `7 Sept` on a modern device, while the coach-approved string is `Hold until 16 Sep`. W7's `dayMon` uses a fixed month table to match the copy; the shipped formatters do not. **This partially reopens B-35** — pinning `en-GB` fixed the day/month *order* but not the month *spelling*, which I did not anticipate when I ruled it. Pick one source of truth for month names and route both through it. | frontend | **built — WO-004 W6.** `dstr()` and every `Intl` call are deleted from `index.html`. `fmt()`/`fmtDay()` are built on `PHAT.dayMon`; weekdays come from a fixed local table. `Number#toLocaleString` went too (`sep()`) — same class of bug |
| B-43 | **Controls below the 44 px minimum, measured at a true 400 px viewport.** The stepper `−`/`+` buttons are **40 px across** — the 46 px figure everyone quotes is their *height*, and the width was never checked. `.showfig` ("movement & cue") is **26 px tall**. These are the two controls used most during a set, by a thumb, with chalk on it. CLAUDE.md §3.6 makes 44 px a hard constraint, so this is a defect, not a nicety. **The redesign's steppers are 38 px wide — worse** — and it adds tabs at ~32 px, header text-buttons at ~14 px and plan-editor controls at 13–20 px. | frontend + ux | **built — WO-004 W6, measured on width.** Stepper keys 44 x 48, inputs >= 48 x 48, `.showfig` 351 x 44, tabs 80 x 52 at 400 px, the Full-volume checkbox 44 x 44 (drawn from tokens, not native). Enumerated by `getBoundingClientRect` on all six routable screens at 400 px and at 200 % text: 0 under 44 in either |
| B-37 | **The ghost text is invisible at the target width.** At 400 px a `.setrow` measures 369 px inside a 366 px `.sets` content box, so the last-session `100×5` hint is squeezed to zero width — on unmarked rows too, so this is pre-existing, not introduced by W6. The single best feature in the app does not render on the phone it was designed for. Measured, with a screenshot. **The redesign fixes this structurally** — the ghost moves to its own full-width row below the steppers, so the zero-width squeeze is impossible. Best single fix in the design. | frontend + ux | **specified — WO-004 W7** |
| B-34 | **`Discard` sits in the top-right corner** of the session header — against CLAUDE.md §3.6 ("nothing important in the top corners"), and it is the most destructive control in the app. W5 gave it a confirmation and a 44 px target, which makes it survivable, not correct. Moving it is a session-view layout change and was out of W4/W5 scope. **The redesign puts it back in the top-right with no confirmation at all** (`PHAT App.dc.html:121`), so this is the moment to move it. | ux + frontend | **specified — WO-004 W5 + W7** |
| B-35 | **Date formatting is locale-driven and now inconsistent.** `toLocaleDateString(undefined, …)` renders `Monday, Sep 7` on a US-locale device where the spec and the handoff brief both write `Tuesday, 7 Sep`. W5's new strings are pinned to `en-GB`; the older `fmt()` is not. Pin `fmt()` too so one screen cannot show two orderings. | frontend | **built — WO-004 W6.** Closed with B-44: one source, `PHAT.dayMon`, and no locale-driven formatter left in `index.html` |
| B-36 | **`#dock` is now `position:fixed`, untested with a software keyboard.** W5 had to make the bottom nav viewport-fixed so the restore sheet was not painted below the fold. On iOS Safari a focused input can lift fixed elements out of place — needs checking on a real phone with the keyboard up on the session view. Desktop and headless are clean. | frontend | open — **needs a real device.** Re-checked headless at 400 px: `#app`’s padding-bottom is still measured from `#dock` (324 px with the offer sheet up) and no input or day row sits under it. The iOS-Safari keyboard case is still unverified and cannot be verified here |
| B-33 | **A notes-only entry inflates the exercise count.** `validateDraft` correctly keeps an entry that has a typed note and no sets — a note is data. But `vTrend` counts entries, so "N exercises" reads one too high. Display-only; fix in the view, **not** by deleting the note. Pinned in `tests.html` as a judgement call. | frontend | **specified — WO-003 W10, ruled in §Decision 7:** an exercise counts when it has ≥ 1 completed set; a notes-only entry is surfaced separately (`5 exercises · 1 note`), never hidden and never deleted. |
| B-31 | Weekday not shown on the day buttons, though the brief fixes them: Mon upper power, Tue lower power, Thu back & shoulders, Fri lower hypertrophy, Sat chest & arms. | frontend | open |
| B-20 | Logic and DOM are interleaved (`verdictFor` reads `S` via `lastFor`). Extract pure functions with explicit arguments so QA can test them without a browser. | backend | **code on `wo-003-advice`: W5 closed the remainder.** `verdictFor`, `lastFor`, `vol`, `topSet` and `done` are gone from `index.html`; the rules are `PHAT.verdict`, `PHAT.vol`, `PHAT.topSet`, `PHAT.round2p5`, `PHAT.workingLoad`, `PHAT.e1rm`, `PHAT.painFlag`, `PHAT.loadWord`, `PHAT.incrementLine`, `PHAT.g1Step`, `PHAT.completedSets` — all callable from `file://` with no DOM. Original entry: Extraction and rule change happen in ONE pass, per §Decision 2: W5 creates the seam with the gate, W6 fills it. No test pins current advice behaviour. After the batch every audit rule must be callable as `PHAT.<fn>` from `file://`. **Partially done by WO-001**: `logic.js` + `window.PHAT` created, holding the date, validation and migration functions. `verdictFor`/`lastFor`/`vol`/`topSet` deliberately left alone; they get extracted in the coach batch (B-06/07/08) so the extraction and the rule change happen once. **Two more extractions requested by QA**, because the storage layer is the most valuable behaviour in WO-001 and none of it is reachable from `tests.html` (an iframe is dead on a `file://` opaque origin): `PHAT.classifyDraftPayload(raw)` → `{status, draft, reason}`, which makes `none`/`parse`/`shape` testable and leaves only `read` manual; and `PHAT.buildSession(draft, dayId, dateStr, id)` returning the exact object handed to `save()`, so W2's "assert on the serialized JSON" criterion stops being proxied through `validateDraft().entries`. |
| B-77 | **A failed plan save borrows the session's copy.** `save()`'s catch (`index.html:1108`) sets `Could not save. Your entries are still on screen — try again.` for every key, so a `SAVE PLAN` that throws talks about "entries" on a screen that has none. What survives is the working copy, which is true and should be said: one sentence for the plan path, ux to word it. Nothing at risk — the working copy is intact (D3/Attack C observed) | ux + frontend | open — WO-006 W4 P3 |
| B-78 | **Phantom `UNSAVED CHANGES` badge if the working-copy clear throws after a successful save.** `SAVE PLAN` writes the plan (returns true) and then removes the working-copy key; if that removal throws, the stored plan is right and the working copy survives equal to it, so the row reads `UNSAVED CHANGES` for a copy with no changes. No data at risk. Fix: judge the badge by comparing the copy to the stored plan, or retry the clear at boot | frontend | open — WO-006 W4 P3 |
| B-79 | **Stale `aria-label` after a rename until repaint.** The editor's rename updates the working copy and the visible text; the control's `aria-label` keeps the old name until the next full render. Screen-reader only, visual is right. Route the label through the same render as the text | frontend | open — WO-006 W4 P3 |
| B-80 | **A restore refusal in `#bk-status` is overwritten within ~2 s by the open-push stamp.** `backupSoon("open")` and the refusal share `S.sync.last`; tap Restore inside the first two seconds of Settings and the refusal is replaced by "Backed up … just now" when that push lands. Nothing written by the restore. Fix: the refusal wins over a later success stamp for one paint, or the push writes its own field | frontend | open — WO-006 W4 P3 |
| B-81 | **Tab B refuses on D10 but shows no session to finish until reload.** With a draft on disk from tab A, tab B's `SAVE PLAN` / `USE THIS PLAN` / Restore correctly refuse with `Finish or discard the session in progress first.` — but tab B's Train screen offers no session to finish or discard, because it booted before the draft existed. The refusal names a thing he cannot see. Cheapest honest fix: the refusal re-reads the draft and the Train screen offers it (the WO-001 offer sheet already exists); or the sentence gains `Reload to see it.` | frontend + ux | open — WO-006 W4 P3 |
| B-82 | **`-1 days ago` on a future-dated demo anchor.** `index.html:1799`'s day-distance copy has no branch for a negative distance, so a sample date ahead of today renders `-1 days ago`. Unreachable with real logging (`today()` is local and never in the future) — only the sample data can produce it. Clamp or say `Tomorrow` | frontend | open — WO-006 W4 P3 |

| B-83 | **The editor cannot move an exercise between days, and the only workaround mints a new id.** `logic.js` has `moveExercise` (within a day) and `moveDay`; nothing crosses a day boundary. To put `d1a` on a "Pull" day he must `removeExercise` + `addExercise`, and `addExercise` mints a fresh opaque id — so `lastFor`, Trend, `speedSource`, `keyLifts` and the six-week check all lose the history. B-46's bug through the one door WO-006 left open. Chady's ask of 2026-09-12 is exactly this: "use the same exercises from PHAT but alternate them … push / pull / legs". Fix: `PHAT.moveExerciseToDay(plan, exId, toDayId, toIndex?)`, exercise object byte-identical, `speedSource`/`keyLifts` untouched, move-then-undo is the identity. **P1**: blocks real use of the editor for the one thing he wants it for | backend + frontend + ux | **specified — WO-007 W3 + W4** |
| B-84 | **No way to delete a day.** Composing three days out of PHAT's five leaves two empty days he cannot remove. Fix: `PHAT.removeDay(plan, dayId)`, **refused unless the day holds zero exercises** — no path removes a slot by removing its day. Drops `reintroOrder[dayId]`; writes nothing to `state.reintro` (a stale counter for a gone day is inert). Control absent, not disabled, on a non-empty day | backend + frontend | **specified — WO-007 W3 + W4** |
| B-85 | **The Plans list leads with `+ Build from empty`.** Chady: "I don't want to build from empty." The route he wants — duplicate PHAT and rearrange — is a `Duplicate this plan` ghost button inside the read-only PHAT view, two taps down. And a plan built from empty has no `derivedFrom`, `speedSource`, `keyLifts` or `cut` tier, so four features are ABSENT on day one. Ruled: **demoted, not deleted** — the primary action becomes `Duplicate PHAT and rearrange`, `Build from empty` stays as a secondary action below it. Removing it entirely is his to say (WO-007 §7) | ux + frontend | **specified — WO-007 W2 + W4** |
| B-86 | **`phatProvenance` is blind to day membership, so a re-split PHAT gets the brief's `[Certain]` diagnosis.** `logic.js:1074` tests `derivedFrom:"phat"` + four key lifts present + their `s/lo/hi` unchanged. A push/pull/legs built by moving slots keeps all three true, so ST1 prints `The split isn't the problem and neither is the diet` about a split the brief never assessed — the exact claim C7b exists to withhold. **P1 candidate the moment B-83 ships.** PM recommendation: provenance drops on any cross-day move or day deletion, generic C7b copy runs. Coach to rule (WO-007 W1 q5) before backend touches it | strength-coach → backend | **open — WO-007 W1 → W3 item 3.** Not reachable on `main` today, because B-83 is not built; must be closed in the same order that builds it |
| B-87 | **The reintro counter is a count per day; a moved `cut` accessory changes what the count means.** `state.reintro[dayId]` is an integer (`counterOf`, `logic.js:5511`) read against `planReintroOrder(plan, dayId)`, which reconciles the coach-authored order with the day's current `cut` list (unlisted cut ids appended in slot order). Move `d3g` from `d3` to a new day and `d3`'s count now points at a different accessory set; the new day's count is 0 whatever `d3g`'s status was. Same class as `removeExercise` shifting a day's list, one step further. Does "back" follow the exercise, the day, or reset — and is the coach's per-day order meaningful on a day it did not compose? | strength-coach → backend | **open — WO-007 W1 q6 → W3.** Default if unruled: a moved accessory goes last in its new day's order |

| B-88 | **A sign-in pushes the device's whole log to whoever signed in.** `runBackup` (`index.html:5461`) builds the payload from disk and upserts it under `S.sync.user.id` with no check that this device's log belongs to that account; `prefs.backup.user` exists (`:5486`, `:5659`) but only gates the unchanged-signature skip. `authTap` schedules the push on every successful sign-in. Sign in as B on A's phone and A's sessions land in B's `sessions` table; B's later Restore on her own phone brings them into her log, Trend, stall report and verdict ghosts. Restore is the mirror: B restoring on A's device replaces A's log (keep kept) with no ownership question. **Reachable today** with `test+e3@example.com` while sign-ups are on. Contamination, not destruction — nothing is deleted — but "a session logged by one user never appears in the other's log" fails. **P1 while both logs are empty; P0 the day either holds a session.** Fix: `PHAT.storeOwner({ownerId,userId,hasData})` pure; refuse the automatic push, the manual push and the restore when the device's stamped owner is a different account and the stores are non-empty; name the owner; write nothing | backend + frontend | **specified — WO-008 W4 + W5.** Built whatever the same-phone answer is. Until it lands: do not sign into a second account on a phone that holds a log |
| B-89 | **Nobody is named on any screen but Settings.** `Signed in as …` renders only inside `vBackupBody`; Home and the session screen say nothing about whose log this is, and first run offers `Start with an empty log` and nothing else — a second phone cannot be claimed by an existing account before its first set. With two people this is how a set lands in the wrong log | ux + frontend | **specified — WO-008 W2 + W5** |
| B-90 | **The app would advise a person it has no numbers for.** `DIET_TARGETS` (`logic.js:6883`, 3,200 / 2,500 kcal), `DIET_WEEKLY` (2,700 maintenance), and `calorieAdvice`'s ladder (`logic.js:4193`, `You are not bulking. Add 200 kcal`) are `handoff-brief.md` §1's 180 cm / 85 kg bulking male, compiled in. There is no person on the data model, so a second account gets Chady's diet and Chady's calorie protocol on day one. Wrong advice by the backlog's own definition. Coach to classify every engine (history-relative / programme-specific / person-specific) and rule what the app may say with no profile — PM position: the diet and calorie layer goes ABSENT (C7a shape), Chady's existing store is stamped so his screens do not change | strength-coach → backend + frontend | **specified — WO-008 W1 → W4 → W5.** First time the app speaks to someone whose numbers it does not have |
| B-91 | **(conditional) Two people on one phone have one set of stores.** `phat:v1:log|bw|draft|prefs|plans|planedit` and `phat:auth` are unkeyed. If Chady and Diana share a device, her session lands in his log. Shape if built: existing keys are profile 0 and no byte moves; a second profile prefixes every key; one pointer key resolved at boot only; switch = pointer write + reload; auth key per profile; each draft on its own key and offered back on switching back. Stop-condition-class change with its own QA gate | backend + frontend | **not dispatched — WO-008 W6.** Exists only if Chady answers "same phone" (WO-008 §7 q1) |
| B-92 | **Two accounts must exist before sign-ups are locked.** `supabase/README.md` §4's runbook is "create Chady's one account, then disable sign-ups". With Diana it is two sign-ups from two phones, then the lock, then delete `test+e3@example.com`, then re-verify RLS from outside with the two **real** accounts (each token 0 of the other's rows on five tables; insert under the other's `user_id` fails `42501`). Wrong order locks her out or leaves the project open | release | **specified — WO-008 W3.** Folds in E-3's two open follow-ups |
| B-93 | **Per-user diet targets.** The honest answer to B-90 for a second account is a profile — goal (bulk / maintain / cut), a maintenance estimate, eight targets — and a way to enter it. That is a diet editor in an app whose Diet tab is labelled `Targets only`. W1 q6 names the minimum; nothing here builds it | strength-coach → ux → backend | **filed — not built.** Needs a coaching ruling on what the app may compute for a person the coach never assessed, and Chady's say on whether the app should hold Diana's numbers at all |

---

## Epics

| ID | Epic | Notes |
|---|---|---|
| E-1 | **GitHub** — init repo, `.gitignore`, first commit, push, branch convention | release-engineer. Needs repo name + visibility from Chady. |
| E-2 | **Vercel** — connect the GitHub repo, static deploy of `main`, preview deploys on branches | release-engineer. Live already, but deployed by API and **not git-linked**, so every release is a manual upload. WO-001 adds a second file (`logic.js`), and a partial manual upload is a black screen — this is now the cheapest risk reduction available. Recommend doing it immediately after WO-001. |
| E-3 | **Supabase** — schema, RLS, auth, and a backup layer that never blocks logging | backend-engineer. **Built on `e3-backup`, 2026-09-11, awaiting QA and merge.** Schema + RLS applied and verified (`supabase/`); `sync.js` + `PHAT.backupPayload` / `restorePayload`; Settings gains a Backup section. **Backup and restore, not two-way sync** — decided, see `docs/decisions.md`. QA owes a `tests.html` section for the pure half and the browser checklist in `supabase/README.md` §7. Open follow-ups: disable sign-ups after Chady's account exists; delete the throwaway test account. |
| E-5 | **Multi-plan** — the plan document, plan-scoped rule metadata, the plans list and the plan editor | backend + frontend. **Done — data half in WO-004 W2/W3, UI half in WO-006, on `main` @ `bad454b` (2026-09-12).** Chady ruled "build it now" on 2026-09-10; the "deferrable" note that used to sit here was the PM's recommendation, not the ruling (B-69). Deliberately not in it: deleting a plan (its own item if he asks), two-way plan sync (restore is still replace-all with a keep). Follow-ups: B-72, B-75, B-76. |
| E-4 | **PWA** — manifest, icons, service worker, installable to the home screen, works with no signal | release-engineer. This is what makes it feel like an app instead of a bookmark. |

## Recommended order

1. ~~B-01, B-02, B-03, B-21~~ — **done. WO-001 merged and deployed.** The 17-item manual checklist is
   still owed on the phone.
2. E-2 — git-link Vercel, so a two-file release stops being a manual upload that can half-fail.
3. B-04 + B-16 — the backup round trip, in one pass. **WO-002.** Reuses WO-001's validators and
   `schemaVersion`, which is why it waits.
4. E-1, E-4 — get it on the phone and installable. Fast, high morale payoff.
5. B-05 — make history correctable.
6. **The advice batch — [WO-003](work-orders/WO-003-advice.md), written and specified.**
   `strength-coach` ruled on all of it in `docs/coach-audit.md`. Closes B-20, B-32, B-33, B-38, B-27,
   B-28, B-41 and B-42 in the same pass. Order, from the audit:
   B-30 + B-17(sort) → B-24 → B-08 + B-25 → B-06 → B-07 → B-22 → B-12 → B-23 → B-26 → B-09 → B-18.
   **One deviation:** the S1 pain *mechanism* moves earlier, into the verdict seam, so no build ever
   makes a confident load recommendation without it (WO-003 §H4).
   **If the batch has to shrink, cut from the end:** B-09 (rest timer), then B-18 (deload). Neither is
   a wrong-advice fix. Everything before them is.
7. **The redesign — [WO-004](work-orders/WO-004-redesign.md), written and specified.** Four milestones.
   **M0 (W1–W5) is not optional and goes first:** the identity decision (B-46, B-47) is free while the log
   is empty and expensive after six weeks of logging. **M1 (W6–W9) is a shippable app** — the new design
   on the session, summary and home screens with all nine engines wired. M2 (W10–W13) completes the tabs.
   M3 (W14–W15, the Plan Editor) was the first thing I recommended cutting; **Chady ruled build it now**
   and it shipped as **WO-006** (`main` @ `bad454b`, 2026-09-12). That ruling was closed and was not
   reopened by a later plan (B-69).
   **PM position, on the record:** if the choice is between finishing WO-004 and logging six sessions, log
   the six sessions. This is the fourth tool built for a programme with zero logged sessions.
8. E-3 — Supabase sync, once the local data model is trustworthy.
9. B-29, B-31 — diet panel and weekday labels. Cheap, do them alongside anything. (B-27 and B-28 ride
   in WO-003 W10 — the new advice copy names those lifts.)
10. Everything else.

## Note on numbering

`B-46`–`B-61` were added by WO-004 from the redesign bundle. **B-46, B-47, B-48 and B-49 are P0** —
four ways the new design loses or corrupts data. All four are ruled, not open questions.


`B-69`–`B-82` were filed by WO-006: B-69 the process errors, B-70–B-72 from the work order, B-73–B-82
from `qa-engineer`'s two W4 passes and `backend-engineer`'s close-out. B-73 and B-74 were P1 and are
done; B-74 was pre-existing since E-3 and is filed as done because it was closed before the release it
was found in shipped. The QA entries in `docs/decisions.md` (2026-09-11 and 2026-09-12) carry the observed
evidence for each.

`B-83`–`B-87` were filed by WO-007 (2026-09-12) from Chady's re-split ask. B-83 is the gap (no cross-day
move), B-84 and B-85 ride with it, B-86 and B-87 are the two advice hazards the move creates and are the
coach's to rule before backend builds them. They sit in the P1 table because B-83 blocks the editor's
only real use and B-86 would have the app vouch for a split the brief never saw.

`B-88`–`B-93` were filed by WO-008 (2026-09-12) from Chady's two-users ask. B-88 is live today (a
sign-in pushes the device's log to whichever account signed in) and is built whatever else is decided;
B-90 is the first time the app would advise a person whose numbers it does not have and is the coach's
first; B-91 exists only if the two of them share a phone; B-93 is filed so the diet editor is not built
by accident inside a login item.

`B-21` is WO-001's zero-weight defect. The `strength-coach` audit proposed its own `B-21`–`B-30`;
those were renumbered to **B-22–B-31** on merge. If you are reading `docs/coach-audit.md` directly,
its "PROPOSED BACKLOG CHANGES" table uses the old numbers — this file is authoritative.
